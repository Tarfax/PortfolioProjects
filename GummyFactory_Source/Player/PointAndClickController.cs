using System;
using System.Collections;
using System.Collections.Generic;
using _Game.Scripts.Actors;
using UnityEngine;
using UnityEngine.AI;

public class PointAndClickController : AstarAI
{/*
    enum MyEnum
    {
        selected,
        idle
    }

    private float rayDistance = Single.MaxValue;
    private Camera camera;


    void Start()
    {
        camera = Camera.main;
    }

    void Update()
    {
        if (Input.GetButton("Fire1"))
        {
            Ray ray = camera.ScreenPointToRay(Input.mousePosition);
            RaycastHit hitInfo;

            if (Physics.Raycast(ray,out hitInfo,rayDistance) == true)
            {
                
            } 
        }
    }*/
}
